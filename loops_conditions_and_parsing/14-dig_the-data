#!/usr/bin/env bash
# This script parses the Apache log file, groups visitors by IP and HTTP status code, and sorts the results by the number of occurrences.

# Use awk to extract IP and HTTP code, sort, count, and format the output
awk '{print $1, $9}' apache-access.log | sort | uniq -c | sort -rn | awk '{print $1, $2, $3}'
